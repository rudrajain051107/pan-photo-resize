{
  "name": "pan-photo-resize",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js",
    "format": "prettier --write .",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
}
